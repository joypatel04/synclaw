# @synclaw/core (planned)

Core (OSS) feature modules live here over time.

Current repo status:
- Core/runtime capability gates are implemented via `lib/edition.ts`.
- Existing app/convex modules are being migrated incrementally.

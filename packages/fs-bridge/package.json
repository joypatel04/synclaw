{
  "name": "@synclaw/fs-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Remote filesystem bridge for Synclaw OpenClaw workspaces",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "tsc -w -p tsconfig.json"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}


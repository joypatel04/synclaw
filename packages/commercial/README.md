# @synclaw/commercial (planned)

Commercial add-on modules live here over time.

Current repo status:
- Commercial capabilities are guarded at runtime/server (`managedProvisioning`, `assistedLaunch`, `billing`).
- Existing app/convex modules are being migrated incrementally.
